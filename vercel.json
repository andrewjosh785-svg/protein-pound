{
  "buildCommand": "npm run build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    { "source": "/meals/:slug", "destination": "/api/meal-page?slug=:slug" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
